/* This program aims to do some calculations. */
#include <stdio.h>

int main(void)
{
	int toes, toesSum, toesSqure;

	toes = 10;

	toesSum = toes + toes;
	toesSqure = toes * toes;

	printf("The values of toes, tow 'toes' and square of toes respectively are %d, %d, %d.\n", toes, toesSum, toesSqure);

	return 0;
}
